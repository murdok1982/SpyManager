import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color backgroundPrimary = Color(0xFF0A0E1A);
  static const Color backgroundSecondary = Color(0xFF111827);
  static const Color backgroundCard = Color(0xFF1A2233);
  static const Color backgroundElevated = Color(0xFF1F2D42);

  // Accents
  static const Color accentCyan = Color(0xFF00D4FF);
  static const Color accentGreen = Color(0xFF00FF87);

  // Semantic
  static const Color danger = Color(0xFFFF3B3B);
  static const Color warning = Color(0xFFFFB800);
  static const Color safe = Color(0xFF00C853);
  static const Color classified = Color(0xFF1565C0);

  // Classification levels
  static const Color unclassified = Color(0xFF607D8B);
  static const Color secret = Color(0xFF8B0000);
  static const Color topSecret = Color(0xFFFFD700);

  // Text
  static const Color textPrimary = Color(0xFFE8EDF5);
  static const Color textSecondary = Color(0xFF8899AA);
  static const Color textMuted = Color(0xFF4A5568);

  // Borders
  static const Color borderSubtle = Color(0xFF2D3748);
  static const Color borderActive = Color(0xFF00D4FF);

  // Overlay
  static const Color overlay = Color(0x80000000);
  static const Color overlayLight = Color(0x40000000);
}

<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Producción: solo HTTPS con CA propia -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">imc.internal</domain>
        <trust-anchors>
            <certificates src="@raw/ca_cert" />
        </trust-anchors>
    </domain-config>
    <!-- Emulador Android en desarrollo: permite HTTP a localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain>10.0.2.2</domain>
        <domain>localhost</domain>
    </domain-config>
    <!-- Todo lo demás: solo HTTPS con CAs del sistema -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
